import React from 'react';
// import { text, number } from '@storybook /addon-knobs';
import { Components } from './components';

export default { title: 'Button' };

export const primary = () => <Components />;
